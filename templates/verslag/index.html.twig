{% extends 'base.html.twig' %}

{% block title %}Verslag index{% endblock %}

{% block body %}
    <h1>Verslag index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titel</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for verslag in verslags %}
            <tr>
                <td>{{ verslag.id }}</td>
                <td>{{ verslag.Titel }}</td>
                <td>
                    <a href="{{ path('verslag_show', {'id': verslag.id}) }}">show</a>
                    <a href="{{ path('verslag_edit', {'id': verslag.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('verslag_new') }}">Create new</a>
{% endblock %}
